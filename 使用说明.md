# 冷冻海产品溯源系统 使用说明书

> 本说明覆盖：环境准备 → 启动 → 三端操作 → 溯源演示 → 常见问题。
> 三端只是**三个前端页面**，统一对接**一个后端**（端口 8080），数据库 MySQL 8.0。

---

## 一、环境准备（只需一次）

| 软件 | 要求 | 本机位置 |
|---|---|---|
| JDK | 17 | `C:\Program Files\Java\jdk-17` |
| Maven | 3.9 | `E:/Maven/apache-maven-3.9.9`（不在 PATH，用全路径） |
| Node.js | 18+ | 本机 v24.12（npm 11.6.2） |
| MySQL | 8.0 | 服务名 `MySQL`，bin 在 `D:\Program Files\MySQL\MySQL Server 8.0\bin` |

> Windows 下推荐用 **Git Bash** 或 **PowerShell** 执行命令。MySQL 客户端不在 PATH，需用全路径或先加环境变量。

---

## 二、初始化数据库（一次）

在 `冷冻海产品溯源系统` 目录下执行（把 `你的密码` 换成 root 密码）：

```bash
MYSQL="D:/Program Files/MySQL/MySQL Server 8.0/bin/mysql.exe"

# 1. 建库
"$MYSQL" -uroot -p你的密码 -e "CREATE DATABASE IF NOT EXISTS seafood_trace DEFAULT CHARSET utf8mb4;"

# 2. 建表（务必带 --default-character-set=utf8mb4，否则中文会报错）
"$MYSQL" -uroot -p你的密码 --default-character-set=utf8mb4 seafood_trace < sql/seafood_schema.sql

# 3. 导入种子数据（省市 / 20家节点企业 / 完整溯源链 / 管理端账号）
"$MYSQL" -uroot -p你的密码 --default-character-set=utf8mb4 seafood_trace < sql/seafood_data.sql
```

> ⚠️ **必须** 用 `--default-character-set=utf8mb4`。否则中文省名/企业名会报 `Data too long for column 'abbreviation'`。

---

## 三、启动后端

```bash
cd seafood-server

# 打包（首次会联网下载依赖，走阿里云镜像，较慢请耐心等）
E:/Maven/apache-maven-3.9.9/bin/mvn -Dmaven.repo.local=E:/Maven/repository -DskipTests package

# 启动（DB 账号密码用环境变量注入，不写进代码）
DB_USER=root DB_PASSWORD=你的密码 java -jar target/seafood-server-1.0.0.jar
```

看到 `Tomcat started on port 8080 ... context path '/api'` 即成功。
后端地址：`http://localhost:8080/api`

> 也可以每次用 `mvn spring-boot:run` 替代 `java -jar`。修改代码后需重新 `package` 再启动。

---

## 四、启动三个前端

分别开三个终端：

```bash
cd seafood-admin-web    && npm install && npm run dev   # http://localhost:3001
cd seafood-node-web     && npm install && npm run dev   # http://localhost:3002
cd seafood-consumer-web && npm install && npm run dev   # http://localhost:3003
```

> 三个前端都会通过 Vite 代理把 `/api` 转发到后端 8080，浏览器无需配置跨域。
> 启动后分别用浏览器打开上面三个地址即可。

---

## 五、登录账号（初始密码均为 `123456`）

| 端 | 账号 | 角色 | 企业 |
|---|---|---|---|
| 管理端 | `admin` | 管理员 | — |
| 流通端 | `breeding001` | 水产养殖企业 | 沈阳浑河水产养殖场（辽宁沈阳） |
| 流通端 | `processing001` | 冷冻加工企业 | 沈阳冷链海产加工厂（辽宁沈阳） |
| 流通端 | `wholesale001` | 批发商 | 顺发海产批发中心（辽宁沈阳） |
| 流通端 | `retail001` | 零售商 | 和平区海丰水产店（辽宁沈阳） |
| 消费者端 | 免登录 | — | 输入溯源码 `NFTS-20240901001` |

其它流通端账号：水产养殖 `breeding002~005`、冷冻加工 `processing002~005`、批发 `wholesale002~004`、零售 `retail002~006`，密码同样 123456。

---

## 六、三端操作指引

### 6.1 系统管理端（`http://localhost:3001`）

1. **登录**：输入 `admin` / `123456`。
2. **可视化大屏**：页面顶部 4 张图实时统计——近 12 个月注册趋势（折线）、注册数量省份分布（饼）、企业类型分布（饼）、省份注册数量（柱）。
3. **节点企业管理**（大屏下方）：
   - **查询**：按 名称（模糊）/ 类型 / 所属省 / 所属市 组合筛选。
   - **清空**：重置筛选条件。 **新建**：弹窗添加企业（选类型、填登录编码、密码默认123456、省份联级下拉）。
   - **详情 / 编辑**：弹窗查看或修改。 **删除**：移除该企业节点。
   - 分页：每页 10 条，底部翻页。

> 管理端只管**企业账号**：流通端所有企业只能由这里注册、增删改、重置信息；流通端自己只能改自己的密码。

### 6.2 流通节点端（`http://localhost:3002`，按登录企业角色显示不同界面）

用对应企业账号登录，例如 `breeding001`。顶部显示**企业名称 + 类型**，底部固定导航：**首页 / 我的 / 更新密码**。

**功能菜单（首页）**：根据角色显示不同入口——
- 水产养殖/冷冻加工/批发：`新建产品批号`、`产品批号管理`、`下游企业进场确认`。
- 零售商：`新建产品批号`、`产品批号管理`（零售是链条末段，无下游可确认）。

**① 新建产品批号**
- **水产养殖企业**：填 产品批号、产品品种、水产品检疫合格证、官方检验员名称。
- **其它企业（加工/批发/零售）**：填 本企业产品批号、产品品种、产品类型，并**关联上游进场信息**：`上游省→市→企业名称→上游产品批号`（四级联动下拉，选批号后自动带入上游品种，该栏禁用）。冷冻加工企业额外填水产品检验检疫合格证、官方检验员。

**② 产品批号管理（列表 Tab 按状态分类）**
- **水产养殖企业**：`待发布 / 已发布` 两个 Tab。
- **其它企业**：`新建 / 待确认 / 已确认` 三个 Tab。
- 操作按钮随状态变化：
  - **待发布/新建**：`更新`、`删除`；养殖多一个 `发布`，其它多一个 `送确认`。
  - **已发布/已确认**：`下架`。
  - `待确认`：只读，等上游企业确认。
  - 点击任意行 → **批号详情页**（零售已确认的批号会显示**专属溯源码**）。

**③ 更新批号**
- 进入后字段与新建一致，批号字段禁用。
- 养殖：勾选 **是否发布**。其它：勾选 **是否向上游发送确认请求**（勾选并保存 → 批号转为 `待确认` 并向上游发起确认）。
- 保存后返回列表，状态按要求流转。

**④ 下游企业进场确认（仅有下游的企业）**
- 例：水产养殖企业登录后，可看到**下游冷冻加工企业**提交的进场批号（状态 `待确认`、其 `所属上游产品批号` 必须是本企业批号）。
- 支持按 **下游企业名称模糊查询**。
- 点 `确认` → 该下游批号转为 `已确认`。
- 零售商下单后，由批发商确认；**批发商确认零售批号时，系统自动生成溯源标识码**。

**⑤ 我的 / 更新密码**
- `我的`：本企业完整信息（名称/类型/省市/地址/营业执照/联系证书等）。
- `更新密码`：输入旧/新密码修改，改完自动退出；页面有 `退出登录` 按钮。

### 6.3 消费者端（`http://localhost:3003`）

1. 首页输入溯源标识码（如 `NFTS-20240901001`），点 `溯源`。
2. 溯源结果页按供应链顺序展示四级节点：
   `水产养殖企业 → 冷冻加工企业 → 批发商 → 零售商`，每级含 企业名称、产品品种、产品批号、检验检疫合格证、产地、批号日期（零售商含溯源码）。

---

## 七、完整溯源演示流程（答辩/演示用）

1. **系统管理端** `admin` 登录 → 大屏看 4 张统计图；在"节点企业"里新建一家企业，展示增删改查。
2. **水产养殖企业** `breeding001` → 新建批号 → 列表 `发布`。
3. **冷冻加工企业** `processing001` → 新建批号（选择关联 沈阳浑河 的批号）→ 列表 `送确认`。
4. 回到 **水产养殖企业** → `下游企业进场确认` → 搜索沈阳冷链 → `确认` → 加工批号变 `已确认`。
5. **批发商** `wholesale001` → 新建批号关联沈阳冷链 → `送确认`；**冷冻加工企业** 确认 → 已确认。
6. **零售商** `retail001` → 新建批号关联顺发 → `送确认`；**批发商** 确认 → 系统**生成溯源码**。
7. 打开 **消费者端**，输入该溯源码 → 展示四级完整链路。

---

## 八、常见问题

| 现象 | 原因与解决 |
|---|---|
| 建库/导入中文报 `Data too long` | 客户端字符集不是 utf8mb4。加 `--default-character-set=utf8mb4` 重导。 |
| 后端启动报 `Field 'password' doesn't have a default value` | 种子 SQL 漏了企业密码列。重新导入 `seafood_data.sql`（已含 password 列）。 |
| 趋势图接口报 `only_full_group_by` | MySQL 严格模式：`GROUP BY` 要用完整表达式。已修复为 `GROUP BY DATE_FORMAT(...)`，无需改动，重打包即可。 |
| 登录提示"登录编码或密码错误" | 账号密码不对；重新导一次种子数据，密码是 `123456`。 |
| `localhost:8080` 被占用 | 改后端 `src/main/resources/application.yml` 的 `server.port`，并同步三个前端 `vite.config.js` 的代理 target 端口。 |
| 前端 3001/3002/3003 被占用 | 改对应 `vite.config.js` 的 `server.port`。 |
| 前端经代理访问后端 404 | 确认后端已启动、代理 `target` 端口与后端一致。 |
| 修改后端不生效 | 需重新 `package` 并重启 `java -jar`。 |
| 前端改了不生效 | dev 模式热更新；若用构建产物需重新 `npm run build`。 |

---

## 九、目录速览

```
冷冻海产品溯源系统/
├── 使用说明.md
├── README.md          # 简版：启动 + 账号
├── sql/               # seafood_schema.sql(建表) / seafood_data.sql(种子)
├── seafood-server/       # 后端（端口8080, context-path=/api）
├── seafood-admin-web/    # 管理端（3001）
├── seafood-node-web/     # 流通节点端（3002）
└── seafood-consumer-web/ # 消费者端（3003）
```

**后端上下文：** 管理员（`/admin/**`）与流通企业（`/node/**`）需登录后携 `Authorization: Bearer <token>` 访问；消费者端 `/open/**` 免登录。token 为 JWT，由 `/auth/login` 签发。
